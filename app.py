"""Streamlit entrypoint for the CCI Commodity Dashboard.

The initial commit only documents the UI blueprint; functional components are
implemented in subsequent commits to keep the git history focused.
"""


def main() -> None:
    """Placeholder main to satisfy import checks before UI logic lands."""
    raise NotImplementedError(
        "Dashboard UI is implemented in a later commit to keep scaffolding separate."
    )


if __name__ == "__main__":
    main()
